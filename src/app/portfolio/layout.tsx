export const metadata = {
  title: 'Portfolio',
  description: 'Track your Sui token balances, swap history, and portfolio analytics in one place with OmniWeave.',
}

export default function PortfolioLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
