export const metadata = {
  title: 'Swap',
  description: 'Swap tokens on Sui at the best rates. OmniWeave finds you the optimal route across all major Sui DEXes.',
}

export default function SwapLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
