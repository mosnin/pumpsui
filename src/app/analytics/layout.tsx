export const metadata = {
  title: 'Analytics',
  description: 'Explore real-time trading analytics across all Sui DEXes aggregated by OmniWeave. Volume, fees, top pairs, and live swaps.',
}

export default function AnalyticsLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
